version: "3"

services:
  concourse-db:
    image: postgres:9.5
    volumes: ["./../postgres-data:/var/lib/postgresql/data"]
    environment:
      POSTGRES_DB: concourse
      POSTGRES_USER: "${CONCOURSE_USERNAME}"
      POSTGRES_PASSWORD: "${CONCOURSE_PASSWORD}"
      PGDATA: /database

  concourse-web:
    image: concourse/concourse:3.0.1
    links: [concourse-db]
    command: web
    depends_on: [concourse-db]
    ports: 
      - "8080:8080"
      - "2222:2222"
    volumes: ["./../keys/web:/concourse-keys"]
    restart: unless-stopped
    environment:
      CONCOURSE_BASIC_AUTH_USERNAME: "${CONCOURSE_USERNAME}"
      CONCOURSE_BASIC_AUTH_PASSWORD: "${CONCOURSE_PASSWORD}"
      CONCOURSE_EXTERNAL_URL: "${CONCOURSE_EXTERNAL_URL}"
      CONCOURSE_POSTGRES_HOST: concourse-db
      CONCOURSE_POSTGRES_USER: "${CONCOURSE_USERNAME}"
      CONCOURSE_POSTGRES_PASSWORD: "${CONCOURSE_PASSWORD}"
      CONCOURSE_POSTGRES_DATABASE: concourse

  concourse-worker:
    image: concourse/concourse:3.0.1
    privileged: true
    links: [concourse-web]
    depends_on: [concourse-web]
    command: worker
    volumes: ["./../keys/worker:/concourse-keys"]
    environment:
      CONCOURSE_TSA_HOST: concourse-web
      CONCOURSE_GARDEN_DNS_SERVER: 8.8.8.8
